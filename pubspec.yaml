name: queens
description: "A universal manga and ranobe reader."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: any
  http: ^1.1.0
  shared_preferences: ^2.2.2
  permission_handler: any
  web_socket_channel: ^2.4.4
  qr_flutter: ^4.1.0
  mobile_scanner: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
